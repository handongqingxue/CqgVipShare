<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
 <mapper namespace="com.cqgVipShare.dao.MessageMapper">
 	<select id="selectCommentListByOpenId" resultType="com.cqgVipShare.entity.Message">
 		select m.id,m.createTime,m.content,md.logo shopLogo,md.shopName,s.name vipName,shr.shareMoney from message m,share_history_record shr,share_vip s,`user` md where m.srUuid=shr.uuid and shr.vipId=s.id and s.shopId=md.id and shr.fxzOpenId=#{fxzOpenId} order by createTime
 	</select>
 </mapper>